这个文件夹中的应用程序由pyinstaller打包，适用于Windows操作系统（已经验证Windows11 64bit可用，其他版本未验证）。使用pyinstaller默认图标，文件名中已标注版本。暂不提供32位版本。The applications in this folder are packaged by PyInstaller and are applicable to the Windows operating system (it has been verified that they are available for Windows 11 64 - bit, while other versions have not been verified). The default icon of PyInstaller is used, and the version is marked in the file name. The 32 - bit version is not provided for the time being.
